#ifndef ALARM_TYPE_H
#define ALARM_TYPE_H

enum AlarmState {
    Active,
    Not_Active,
    Acknowledge
};

enum AckMode{
    Manual,
    Auto
};

enum Analog_Mode{
    HiHI,
    HI,
    LO,
    LOLO
};
















#endif